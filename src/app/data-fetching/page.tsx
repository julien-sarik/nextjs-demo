// `app/data-fetching/page.tsx` is the UI for the `/data-fetching` URL
export default function Page() {
    return <>
      <h1>Data fetching</h1>
      <p>This section describes data-fetching patterns in Next.js.</p>
    </>
}