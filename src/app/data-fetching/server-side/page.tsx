// `app/data-fetching/server-side/page.tsx` is the UI for the `/data-fetching/server-side` URL
export default function Page() {
    return <>
      <p>Next.js recommend to use server-side components for data-fetching but I&apos;m all for static websites.</p>
    </>
}